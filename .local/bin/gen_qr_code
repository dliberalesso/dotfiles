#!/usr/bin/env zsh
# vim:syntax=zsh
# vim:filetype=zsh

file="$HOME/qrcode.png"

while read string && [ $string != "quit" ]
do
  qrencode -o $file -s 7 $string
  nircmd.exe clipboard copyimage `wslpath -aw $file`
  rm $file
done
